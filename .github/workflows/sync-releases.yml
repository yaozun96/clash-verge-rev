name: Sync Upstream Releases

on:
  workflow_dispatch:
  schedule:
    - cron: '0 3 * * *'

permissions:
  contents: write

jobs:
  sync_releases:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Sync Releases
        uses: aminya/sync-releases@v1
        with:
          source: "clash-verge-rev/clash-verge-rev@dev"
          destination: "yaozun96/clash-verge-rev"
          token: ${{ secrets.GITHUB_TOKEN }}
